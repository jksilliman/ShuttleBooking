-title "Sign in"

%Signing in is temporarily disabled while we update are servers. We hope to enabled it again shortly!
%p= markdown(SiteContent.get("sign_in_text"))

= simple_form_for(resource, :as => "User", :url => user_session_path) do |f| 
  = f.input :username, :label => "CNetID"
  = f.input :password

  = f.submit "Sign in", :class => "btn primary"

